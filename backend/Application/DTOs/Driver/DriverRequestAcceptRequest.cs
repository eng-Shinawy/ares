namespace Backend.Application.DTOs.Driver
{
    public class DriverRequestAcceptRequest
    {
        // Currently empty since accepting doesn't require extra fields, but future-proofs the API.
    }
}
