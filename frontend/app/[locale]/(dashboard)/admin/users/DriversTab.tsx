"use client";

// DriversTab — wraps AdminDriversClient directly.
// The component is already a complete, self-contained "use client" component
// so we just re-export it with the tab-friendly name.
export { default } from "../drivers/AdminDriversClient";
// test pull request 2